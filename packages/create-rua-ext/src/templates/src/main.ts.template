import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import {ToastProvider, Container, Background} from "@rua/ui";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ToastProvider>
        <Container>
          <Background>
            <App/>
          </Background>
        </Container>
    </ToastProvider>
  </React.StrictMode>
);
